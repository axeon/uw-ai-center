project:
  name: @project.name@
  version: @project.version@

spring:
  application:
    name: ${project.name}
  cloud:
    nacos:
      server-addr: ${NACOS_SERVER}
      username: ${NACOS_USERNAME}
      password: ${NACOS_PASSWORD}
      config:
        namespace: ${NACOS_NAMESPACE}
        file-extension: yaml
      discovery:
        namespace: ${spring.cloud.nacos.config.namespace}
        service: ${spring.application.name}
        ip: ${APP_HOST:}
  # Elasticsearch 配置（RAG向量检索用），AiEsConfiguration依赖此处自动配置的RestClient Bean
  elasticsearch:
    uris: http://192.168.88.21:9200
    # 如ES开启了认证，取消下面注释填写账号密码
    username: elastic
    password: Few20Dk1zuSVZcdzxhOYMG1kp95XVzPj
    connection-timeout: 30s
    socket-timeout: 60s
